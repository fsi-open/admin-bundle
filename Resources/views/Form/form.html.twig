{% extends admin_templates_base %}

{% block themes %}
    {% form_theme form admin_templates_form_theme %}
{% endblock themes %}

{% block content %}
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-body">
                <h3 id="page-header">{{ 'crud.form.title'|trans({}, 'FSiAdminBundle') }}</h3>
                {{ dump(id) }}
                {{ form_start(form, { action: path('fsi_admin_form', { element: element.id, id: app.request.get('id') }), 'attr': { class: 'form-horizontal' }|merge(form.vars.attr) }) }}
                    {{ form_widget(form) }}
                    <div class="form-group">
                        <div class="col-lg-offset-2 col-lg-10">
                            <button type="submit" class="btn btn-default">{{ 'crud.form.button.save'|trans({}, 'FSiAdminBundle')  }}</button>
                            {% if app.request.query.has('redirect_uri') %}
                            <a class="btn btn-info pull-right" href="{{ app.request.query.get('redirect_uri') }}">{{ 'crud.form.button.back'|trans({}, 'FSiAdminBundle')  }}</a>
                            {% endif %}
                        </div>
                    </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}